# Nous-approved MCP catalog entry.
# Presence in this directory = approval. Merged via PR review.
manifest_version: 1

name: paypal
description: >-
  Payments, invoices, and subscriptions via PayPal's hosted MCP.
source: https://developer.paypal.com/tools/mcp-server/

# Official vendor-hosted remote MCP (URL-only — SOCIS never spawns a local
# process for this entry). Native OAuth 2.1 + Dynamic Client Registration;
# SOCIS's MCP client + mcp_oauth_manager handle discovery, PKCE, token
# exchange, and refresh.
transport:
  type: http
  url: https://mcp.paypal.com/sse

auth:
  type: oauth

# Composer-suggestion triggers (desktop brand pills).
suggest:
  keywords:
    - paypal
  hosts:
    - developer.paypal.com

post_install: |
  On first connection SOCIS opens a browser to authorize with
  Paypal (or run `socis mcp login paypal`). Approve access,
  then restart the session so tools load.
