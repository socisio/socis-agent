#!/usr/bin/env python3
"""
SOCIS Agent CLI launcher.

This wrapper should behave like the installed `socis` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from socis_cli.main import main
    main()
